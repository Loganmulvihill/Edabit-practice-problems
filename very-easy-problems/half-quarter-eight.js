function halfQuarterEighth(n) {
  let array = [];
  const half = n / 2;
  const quarter = n / 4;
  const eigth = n / 8;
  array.push(half);
  array.push(quarter);
  array.push(eigth);
  return array;
}
